#ifndef UNTITLED17_APP_H
#define UNTITLED17_APP_H

/**
 * @brief Represents the main application class for this project.
 *
 * The App class serves as the entry point for the application and contains
 * the static run method responsible for executing the main application of the project.
 */
class App {

public:
    /**
     * @brief Executes the main functionalities of the application.
     *
     * The run method serves as the entry point for executing the primary functionalities
     * of the application. It initializes necessary components and starts the execution
     * of the application.
     */
    static void run();
};

#endif //UNTITLED17_APP_H
